// Utilização do code snippets "tsrfce" para criar um componente utilizando a extensão "Typescript React code snippets"

type Props = {}

// Cria e exporta o componente
export default function FirstComponent({}: Props) {
  return (
    <>
      <p>Este é o primeiro componente.</p>
    </>
  )
}